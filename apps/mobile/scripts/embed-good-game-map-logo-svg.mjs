#!/usr/bin/env node
/**
 * Regenerates `src/components/goodGameMapLogoSvgXml.ts` from `assets/brand/good-game-logo-objects.svg`.
 * Run from apps/mobile: `node scripts/embed-good-game-map-logo-svg.mjs`
 */
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const root = path.join(__dirname, "..");
const svgPath = path.join(root, "assets/brand/good-game-logo-objects.svg");
const outPath = path.join(root, "src/components/goodGameMapLogoSvgXml.ts");
const svg = fs.readFileSync(svgPath, "utf8");
const out =
  "// Generated by scripts/embed-good-game-map-logo-svg.mjs\n" +
  "export const GOOD_GAME_MAP_LOGO_SVG_XML = " +
  JSON.stringify(svg) +
  ";\n";
fs.writeFileSync(outPath, out);
console.log("Wrote", outPath, `(${out.length} bytes)`);
